---
id: TKT-005
title: "Structured logging migration (os.Logger)"
type: enhancement
status: open
priority: high
branch: ""
created: 2026-04-07
updated: 2026-04-07
---

## Description
Replace 20 scattered `print()` calls in `RemoteDeploy/` with `os.Logger` organized by subsystem (`com.remotedeploy.server`, `.build`, `.api`, `.tailscale`, `.pairing`). iOS companion already uses `os.Logger` in some places — extend everywhere. Add log levels (debug/info/error). Document how to enable in Console.app.

## Acceptance Criteria
- [ ] Zero `print()` calls remain in production code under `RemoteDeploy/` and `RemoteDeployCompanion/`
- [ ] Each subsystem has a dedicated `Logger` extension
- [ ] HTTPHandler logs request method/path/status/duration
- [ ] AuthMiddleware logs failed auth attempts
- [ ] Build engine, Tailscale provider, paired device store all use logger
- [ ] Privacy annotations on user data (`\(value, privacy: .private)`)
- [ ] README or HOW-TO-USE updated with Console.app filter instructions
