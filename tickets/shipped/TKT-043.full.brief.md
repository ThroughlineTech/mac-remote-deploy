# Brief: Full Lifecycle for TKT-043

> **For the human, when you come back to this:** This is a delegation brief generated by Claude Code's `/ticket-delegate TKT-043`. To execute it, open Copilot Chat in this project, select your model, and run `/run-brief tickets/TKT-043.full.brief.md`. When the agent finishes, come back to Claude Code and run `/ticket-collect TKT-043`.

## Your role

You are taking this ticket from `open` to `review` — the full lifecycle. You will investigate the codebase, design a solution, implement it, write tests, and commit working code on a feature branch. You will NOT merge to main and you will NOT deploy — those happen later.

This is your ticket. Approach it with your own perspective — investigate freely, design the solution you think is best, implement it your way. The acceptance criteria are the contract; how you get there is up to you.

## Ticket

**ID:** TKT-043
**Title:** Companion Build tab stuck on "Building…" after build completes
**Type:** bug
**Branch:** `ticket/tkt-043-companion-build-tab-stuck` (already created for you)

> **Sequential batch note:** This is part of a 2-ticket sequential batch with TKT-044. Before starting, verify you're on this branch: `git checkout ticket/tkt-043-companion-build-tab-stuck`. The other ticket (TKT-044) touches related companion-app build-status code — do not reach into TKT-044's concerns.

### Description

In the iOS companion app's **Build** tab, after tapping **Build & Deploy** the UI enters the "Building…" state with a ticking elapsed-time counter under the button. When the build finishes on the Mac, the Prowl push notification arrives on the phone (so the server-side build definitely completed and the notification path works), but the Build tab still shows "Building… 63s" (or whatever the final time is) and the counter keeps ticking indefinitely.

The companion never transitions out of the in-progress state, so:
- The Build & Deploy button stays disabled
- The spinner/ticker never stops
- The build log area shows only "Command line invocation:" (the initial line) and never receives the rest of the log
- There's no success / failure indication

Expected behavior: when the server reports the build as finished (success or failure), the companion Build tab should update to the finished state — stop the timer, re-enable the button, and show the final log + status.

Likely causes to investigate:
- WebSocket connection for live build logs dropping or never receiving the terminal "build finished" event
- Companion polling/subscription loop not being notified when build state transitions on the server
- Build status response from `/api/v1/projects/:id/build` not being consumed after kickoff

### Reproduction Steps

1. Open the companion app, go to the **Build** tab
2. Select the `rejog-ios` project
3. Tap **Build & Deploy**
4. Wait for the Prowl push notification indicating the build finished
5. Observe: the Build tab still shows "Building… {N}s" with the timer still incrementing, button still disabled, log area empty beyond the first line

### Acceptance Criteria

- [ ] When a build completes on the Mac (success or failure), the companion Build tab transitions out of the "Building…" state
- [ ] The elapsed-time ticker stops at the final duration
- [ ] The Build & Deploy button becomes enabled again
- [ ] The log area shows the build log (or at minimum a success/failure indicator)
- [ ] This works whether or not the phone was actively in the foreground while the build ran

## Project rules (from CLAUDE.md and global config)

- **Commit format:** `TKT-043: short description`
- **No Claude branding in commits.** Do not add `Co-Authored-By: Claude` trailers or "Generated with Claude Code" lines.
- **Don't merge or deploy.** Implementing the fix is one thing; merging/shipping is a separate, explicit step.
- **Read before writing.** Don't propose changes to code you haven't read.
- **Tickets are the system of record.** Keep `tickets/TKT-043.md` updated as you work.
- Only add comments where the logic isn't self-evident. Don't add docstrings/type annotations to code you didn't change.
- Don't add error handling or fallbacks for scenarios that can't happen. Trust internal guarantees.

## Project commands (from .claude/ticket-config.md)

- **Test:** `xcodebuild test -project RemoteDeploy.xcodeproj -scheme RemoteDeploy -destination 'platform=macOS'`
- **Build:** `xcodebuild build -project RemoteDeploy.xcodeproj -scheme RemoteDeploy -destination 'platform=macOS'`
- **Lint:** (none)

> **Important:** The companion is an iOS app, not the macOS host. To verify companion changes compile, you likely need a separate `xcodebuild` invocation against the `RemoteDeployCompanion` scheme with an iOS simulator destination, e.g.:
> ```
> xcodebuild build -project RemoteDeploy.xcodeproj -scheme RemoteDeployCompanion -destination 'generic/platform=iOS Simulator'
> ```
> If the project doesn't exist yet, run `xcodegen generate` first (the project uses XcodeGen; `project.yml` is the spec).

## Key source locations (companion app)

**Likely to touch (Build tab bug):**
- `RemoteDeployCompanion/Views/Build/BuildControlView.swift` — the Build tab UI (the screen showing "Building… 63s")
- `RemoteDeployCompanion/Services/BuildManager.swift` — tracks in-flight build state, elapsed time, log accumulation
- `RemoteDeployCompanion/Services/WebSocketClient.swift` — WebSocket subscription for live build log + state events from the Mac host
- `RemoteDeployCompanion/Services/APIClient.swift` — REST client for `/api/v1/projects/:id/build` (kickoff + status)
- `RemoteDeployCompanion/Services/ConnectionManager.swift` — owns the connection to the Mac; WebSocket lifecycle may live here

**Host-side event producer (read-only, for understanding the wire protocol):**
- `RemoteDeploy/API/WebSocket/` — host's WebSocket manager and handler; look for the "build finished" / terminal event it emits so you know what the companion should be listening for
- `RemoteDeploy/API/Routes/` — `/api/v1/projects/:id/build` route handler

**Shared types (what the WebSocket messages look like on both sides):**
- `Packages/RemoteDeployShared/Sources/` — DTOs used by both the host and the companion

**Tests:**
- `RemoteDeployTests/` — host unit tests
- `RemoteDeployIntegrationTests/` — real NIO server integration tests (useful to verify what the host actually emits on build finish)
- `RemoteDeployCompanionUITests/` — companion UI tests (if any existing build-tab tests, add to them)

## Context docs

- `README.md` — architecture overview
- `docs/remote-deploy-server-spec.md` — full technical spec, REST + WebSocket

## What to do

### Phase 1: Investigate

1. Read the description and acceptance criteria above carefully.
2. Read every file in "Key source locations" end-to-end. Do not skim.
   - Pay special attention to `BuildManager.swift` — it almost certainly owns the `@Published` state that drives the Build tab's "Building…" view, including the timer.
   - In `WebSocketClient.swift`, verify how incoming messages are decoded and dispatched. Does the companion actually handle a terminal "build finished" event? What message type does the host send?
   - Check `BuildControlView.swift` — is it observing `BuildManager` via `@ObservedObject` / `@StateObject` / `@EnvironmentObject`? Is the timer driven by a `Timer.publish` that's never cancelled?
3. Read the host side (RemoteDeploy/API/WebSocket/ and Routes/) to understand exactly what the host sends when a build finishes. The Prowl notification works, so the host definitely knows the build finished — but does it emit a WebSocket message for the same event, and if so what does that message look like?
4. Trace the end-to-end path: host finishes build → (Prowl notif emitted) + (WebSocket emit?) → companion WebSocketClient receives → BuildManager updates → BuildControlView re-renders. Find where the chain is broken.
5. Identify regression risk.

Write your findings into `tickets/TKT-043.md`:

**`## Investigation`** — what you found, with specific file paths and line numbers. Root cause. Regression risk (low/medium/high + why).

**`## Proposed Solution`** — your approach, why this over alternatives, tradeoffs.

**`## Implementation Plan`** — numbered checklist. Each step names the file, function/component, and exactly what changes.

### Phase 2: Implement

1. Verify you're on branch `ticket/tkt-043-companion-build-tab-stuck`. If not, stop and report.
2. Work through your Implementation Plan item by item:
   - Read every file before modifying it
   - Follow existing patterns in the codebase
   - Follow project rules from above
3. Add tests following existing conventions (prefer adding to `RemoteDeployCompanionUITests/` or a companion unit test target if one exists; otherwise add integration tests on the host side that verify the WebSocket emits the terminal "build finished" event with the right shape).
4. Run the build/test commands. ALL tests must pass. Build (both RemoteDeploy and RemoteDeployCompanion schemes) must complete cleanly.

### Phase 3: Commit and document

1. Stage files specifically (`git add path/to/file`, NOT `git add -A`)
2. Commit with format: `TKT-043: {short description}`
3. Push the branch: `git push -u origin ticket/tkt-043-companion-build-tab-stuck`
4. Update `tickets/TKT-043.md`:
   - **`## Files Changed`** — list every file created or modified with a one-line description
   - **`## Test Report`** — tests added, total passing, build status, any warnings
   - Set `status: review`
   - Update the `updated:` date

## Hard rules

- DO NOT merge to main
- DO NOT deploy
- DO NOT change to a different branch mid-work
- DO NOT push tags
- DO NOT touch files unrelated to this ticket
- DO NOT modify TKT-044's ticket file or its brief
- Scope discipline: this ticket is about the **Build tab** being stuck. TKT-044 is about the **Projects detail view** not updating. Don't fix both here — keep the changes focused on the Build tab path. If you find shared infrastructure that also fixes TKT-044, note it in your Investigation section but don't expand scope.
- Every commit must be atomic and buildable
- If you hit a wall, STOP. Set the ticket status to `proposed` with notes on what went wrong.

## When done

Output: `Brief executed: TKT-043.full.brief.md. Hand back to Claude Code with: /ticket-collect TKT-043`

Include in your output:
- Regression Risk: low/medium/high
- Number of commits made
- Number of files changed
- Number of tests added
- All tests passing: yes/no
- Build clean (both schemes): yes/no
- Any concerns or things you'd flag for the reviewer
