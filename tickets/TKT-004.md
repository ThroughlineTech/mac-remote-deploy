---
id: TKT-004
title: "Inject route handler dependencies via protocols"
type: enhancement
status: open
priority: high
branch: ""
created: 2026-04-07
updated: 2026-04-07
---

## Description
Route handlers in `RemoteDeploy/API/Routes/` are constructed inline in `configureAPIRouter()` ([RemoteDeployApp.swift:332-372](RemoteDeploy/RemoteDeployApp.swift#L332-L372)) with hardcoded closures (e.g. `schemeDetector`, `buildCanceler: { false }`, `buildHistoryProvider: { [] }`). This blocks unit testing of individual handlers. Extract closures into protocols, build a factory, allow mock injection in tests. Prerequisite/companion to TKT-003.

## Acceptance Criteria
- [ ] New protocols: `SchemeDetecting`, `BuildTriggering`, `BuildCanceling`, `BuildHistoryProviding`, `BuildStatusProviding`, `SettingsUpdating`
- [ ] Real implementations live alongside existing services
- [ ] `configureAPIRouter()` uses an injectable factory
- [ ] Each route handler accepts only protocol-typed dependencies
- [ ] Mocks added under `RemoteDeployTests/Mocks/`
- [ ] Existing tests still pass
