---
id: TKT-045
title: "Companion Build tab stays stuck when xcodebuild fails at preflight (pre-build config error)"
type: bug
status: proposed
priority: medium
app: macos
branch: ""
created: 2026-04-11
updated: 2026-04-11
---

## Description

TKT-043 fixed the companion Build tab getting stuck in the "Building…" state for successful/failed builds that actually reached the compile phase — by making `BuildControlView` / `BuildLogStreamView` observe `BuildManager` and `WebSocketClient` as `@EnvironmentObject` directly, so terminal `buildstatus` WebSocket frames re-render the view in-place.

But there's a second, related failure mode that TKT-043's fix may not cover: **xcodebuild preflight errors that fail the archive before any compile work begins.**

Concrete example observed 2026-04-11: a project was misconfigured in the companion's project store with `scheme=RemoteDeploy` + `destination=generic/platform=iOS`. The RemoteDeploy scheme is macOS-only, so `xcodebuild archive` failed immediately with:

```
xcodebuild: error: Unable to find a destination matching the provided destination specifier:
    { generic:1, platform:iOS }
```

This happens *before* xcodebuild starts the "Command line invocation" log streaming that the companion observes. In the companion Build tab during this failure, the symptom matches the original TKT-043 bug: "Building… 63s" with the elapsed counter still ticking, log area showing only the initial line (or nothing), button stuck disabled. The Prowl notification *may or may not* fire depending on how the host's `BuildEngine` and notification plumbing handle pre-build failures.

The question this ticket needs to answer: **does the host emit a terminal `buildstatus` frame (state=`failure`) when xcodebuild exits non-zero at the destination-specifier / scheme-validation stage, before any output is produced?** If yes, TKT-043's fix should already handle it and this ticket can be closed as already-fixed. If no, there's a gap in the host's `XcodeBuildEngine` error-path signaling, and the companion's Build tab will still get stuck in the `Building…` state forever for this class of failure.

## Reproduction Steps (bugs only)

1. In RemoteDeploy (Mac app) → Settings → Projects, create or edit a project entry with a deliberately invalid scheme/destination combination:
   - Scheme: `RemoteDeploy` (a macOS-only scheme)
   - Platform/Destination: iOS
2. Open the companion app, go to the **Build** tab, select this project
3. Tap **Build & Deploy**
4. Observe the xcodebuild invocation fail at the destination check within a second or two (no log output, no compile phase)
5. Check the companion Build tab:
   - Expected: transitions to a failure state with the error message, button re-enabled, timer stopped
   - Observed (hypothesis): stays in "Building… Ns" indefinitely, log area empty, button stuck disabled — same symptom as the original TKT-043 bug

## Acceptance Criteria

- [ ] When `xcodebuild archive` fails at preflight (destination mismatch, invalid scheme, missing DEVELOPMENT_TEAM, etc.), the companion Build tab transitions out of the "Building…" state
- [ ] The failure is visible to the user on the companion — either a specific error message from xcodebuild's stderr, or a generic "Build failed to start" indicator with the exit code
- [ ] The elapsed-time ticker stops at the final duration
- [ ] The Build & Deploy button becomes re-enabled
- [ ] The fix handles at least these preflight failure modes:
  - Invalid destination (scheme/platform mismatch)
  - Scheme does not exist in the project
  - Project file cannot be opened
- [ ] The same preflight failure also produces a Prowl (and any other configured push) notification that matches the on-screen error message, so the UI and notification channels stay consistent


---
<!-- Everything below is filled by the agent. Do not edit manually. -->

## Investigation

### End-to-end chain for a preflight failure (by code inspection)

When `xcodebuild archive` is invoked with a scheme/destination mismatch, it exits non-zero almost immediately (typically within 1–2 seconds, before any compile work). Tracing how that exit propagates through the stack:

1. **[RemoteDeploy/Services/XcodeBuildEngine.swift:273](RemoteDeploy/Services/XcodeBuildEngine.swift#L273) — `runXcodebuild`.** The process is launched with stdout/stderr pipes and readability handlers that call `emitLog` for every incoming line. On termination ([XcodeBuildEngine.swift:321-329](RemoteDeploy/Services/XcodeBuildEngine.swift#L321-L329)):
   ```swift
   } else if proc.terminationStatus != 0 {
       let msg = "xcodebuild exited with code \(proc.terminationStatus)"
       self?.setStatus(.failure(error: msg))
       continuation.resume(throwing: BuildError.xcodebuildFailed(proc.terminationStatus, msg))
   }
   ```
   The engine **does** detect the non-zero exit, sets its local `.failure` status, and throws `BuildError.xcodebuildFailed`. The throw propagates up through [`XcodeBuildEngine.build(project:)`](RemoteDeploy/Services/XcodeBuildEngine.swift#L146).

2. **[RemoteDeploy/Managers/BuildManager.swift:196-226](RemoteDeploy/Managers/BuildManager.swift#L196-L226) — `triggerBuild` `catch` block.** The thrown error is caught:
   ```swift
   } catch {
       let endTime = Date()
       logTask.cancel()
       setBuildStatus(.failure(error: error.localizedDescription))
       ...
       notificationManager.notifyBuildFailure(...)
       await sendPushNotification?("Build Failed", ..., .high, nil)
   }
   ```
   `setBuildStatus` at [BuildManager.swift:89-92](RemoteDeploy/Managers/BuildManager.swift#L89-L92) both assigns `@Published var buildStatus` **and** calls `buildEventBroadcaster?.broadcastBuildStatus(new)`. So a terminal `.failure` frame is broadcast automatically on every error-path transition.

3. **[RemoteDeploy/Services/NIODeployServer+BuildEventBroadcasting.swift:21-28](RemoteDeploy/Services/NIODeployServer+BuildEventBroadcasting.swift#L21-L28) — `broadcastBuildStatus`.** Maps the internal `BuildStatus.failure(error:)` to `BuildStatusInfo(state: "failure", message: error)`, JSON-encodes it, and calls `webSocketManager.broadcast(type: "buildstatus", payload: json)`. All subscribed WebSocket clients receive the terminal frame.

4. **[RemoteDeployCompanion/Services/BuildManager.swift:109-134](RemoteDeployCompanion/Services/BuildManager.swift#L109-L134) — `observeWebSocketStatus`.** The companion's sink explicitly handles both terminal states:
   ```swift
   if status.state == "success" || status.state == "failure" {
       self.isBuilding = false
       self.pollTask?.cancel()
   }
   ```

5. **[RemoteDeployCompanion/Views/Build/BuildControlView.swift](RemoteDeployCompanion/Views/Build/BuildControlView.swift) — TKT-043's fix.** With `@EnvironmentObject var buildManager: BuildManager`, the Build tab now reacts directly to the `isBuilding = false` assignment above and re-renders in-place.

### Conclusion from code inspection

**The chain is correct.** Every link from `xcodebuild` exiting non-zero at preflight to the companion Build tab re-rendering exists and has a sensible implementation. TKT-043's fix should, in theory, cover this failure mode: the same `buildstatus` WebSocket frame that unsticks the UI for compile-phase failures will also unstick it for preflight failures, because it's the same broadcast path on the host side.

**Existing test coverage confirms the BuildManager side.** [RemoteDeployTests/BuildManagerBroadcastTests.swift:152](RemoteDeployTests/BuildManagerBroadcastTests.swift#L152) — `test_triggerBuild_failurePath_broadcastsBuildingThenFailure` — stubs `MockBuildEngine.buildShouldThrow` with a synthetic `LocalizedError` and verifies the broadcaster receives a terminal `.failure(...)` call. This proves the BuildManager catch → broadcast path works for *any* thrown error, regardless of source. A real `XcodeBuildEngine.xcodebuildFailed` throw is just another `LocalizedError` through the same codepath.

### What's NOT currently tested

- No integration test launches a **real** `XcodeBuildEngine` against a bad scheme/destination and asserts that `build(project:)` throws `BuildError.xcodebuildFailed` with a non-zero exit code. The cancellation tests ([RemoteDeployTests/XcodeBuildEngineCancelTests.swift](RemoteDeployTests/XcodeBuildEngineCancelTests.swift)) cover the signal/kill path but not the exit-status propagation path for a naturally-failing invocation.
- No end-to-end test reproduces: "launch real xcodebuild with bogus args → engine throws → BuildManager broadcasts `.failure` on the live `NIODeployServer` → subscribed WS client sees the terminal frame." [RemoteDeployIntegrationTests/BuildManagerWebSocketIntegrationTests.swift](RemoteDeployIntegrationTests/BuildManagerWebSocketIntegrationTests.swift) (extended by TKT-043) covers the WS broadcast shape using a `server.broadcastBuildStatus(.failure(error: "code sign error"))` call directly, not a real build.

### Secondary issues found while tracing the chain

1. **Error message is not useful for the user.** When xcodebuild exits non-zero, the only information reaching the companion is `BuildError.xcodebuildFailed(code, "xcodebuild exited with code N")`. The actual xcodebuild stderr lines (`xcodebuild: error: Unable to find a destination matching the provided destination specifier: { generic:1, platform:iOS }`) are streamed to the log via `emitLog("[stderr] ...")` and will appear in `buildLog`, but the short error string that ends up in the `.failure` status / push notification / Prowl title is generic. From a UX standpoint, the push notification will say "xcodebuild failed (exit 70): xcodebuild exited with code 70" — unhelpful. The engine should capture the last handful of stderr lines and include them in the thrown error's message.

2. **Potential stderr-ordering race.** At [XcodeBuildEngine.swift:295-301](RemoteDeploy/Services/XcodeBuildEngine.swift#L295-L301), the stderr `readabilityHandler` is a closure that fires on an unspecified queue. The `terminationHandler` ([line 304](RemoteDeploy/Services/XcodeBuildEngine.swift#L304)) clears the handlers ([lines 306-307](RemoteDeploy/Services/XcodeBuildEngine.swift#L306-L307)) — which could theoretically race a final stderr read-out still in flight and lose the last stderr line. For a preflight failure where *all* the interesting stderr comes out right before the exit, this is a plausible source of a "missing error message" symptom. Not a stuck-UI bug per se, but worth fixing at the same time.

3. **Log stream finish timing.** `BuildManager.triggerBuild` at [line 150/198](RemoteDeploy/Managers/BuildManager.swift#L198) calls `logTask.cancel()` before `setBuildStatus(.failure(...))`. Since `logTask` runs on `@MainActor` same as the catch block, the cancel happens synchronously, so in-flight `broadcaster.broadcastBuildLog(line)` calls that were already executing in the task's iteration are NOT stopped mid-call. The cancel just stops *further* iterations. The `for await` loop sees `Task.isCancelled` at the next iteration boundary and exits. For preflight failures where the last stderr line is the informative error, there's a small window where it might be emitted but not consumed before `logTask.cancel()` fires. Low risk, worth a defensive `continuation.finish()` on the engine's log continuation in the error termination path to flush pending lines deterministically.

### Regression Risk

**Low-to-medium.** The "fix" for the primary bug (if it manifests) is additive — better error messages, explicit log-stream draining on error, and test coverage. None of it changes the happy path or the existing broadcast shape. The only subtle area is stderr capture: a regression here would affect error messaging for all xcodebuild failures, not just preflight ones, so a misstep would be broadly noticeable and caught quickly.


## Proposed Solution

**Phase A — verify the primary bug exists.** Before writing any code, reproduce the exact preflight scenario on a real device with the post-TKT-043 companion. If the Build tab transitions correctly, Phase A is the whole fix: close this ticket as verified and only land the Phase B improvements as their own small ticket (or skip entirely).

**Phase B — regardless of A, ship the secondary improvements.** These are independent of whether the primary bug manifests:

1. **Capture stderr tail in the thrown error message.** Give `XcodeBuildEngine` a small ring-buffer (last 8 lines) that the stderr readability handler appends to as it streams. When the termination handler sees a non-zero exit, include the ring-buffer contents in `BuildError.xcodebuildFailed`'s message. The downstream companion and Prowl notification will then show the actual xcodebuild error line ("Unable to find a destination matching…") instead of "exit code 70".

2. **Deterministic log-stream drain on error.** In the error branch of [BuildManager.swift:196-226](RemoteDeploy/Managers/BuildManager.swift#L196-L226), before `logTask.cancel()`, explicitly finish the engine's log stream so any in-flight stderr lines flush to the broadcaster before the task exits.

3. **Integration-test coverage for real xcodebuild preflight failure.** Add one test under `RemoteDeployTests/XcodeBuildEngineCancelTests.swift` (renaming the file is out of scope) or a new `XcodeBuildEngineFailureTests.swift` that runs a real `xcodebuild archive` against `/Applications/Xcode.app/Contents/Developer/../not-a-real-project.xcodeproj` (or a scheme name that doesn't exist in the current project) and asserts:
   - The call throws `BuildError.xcodebuildFailed`
   - The thrown error's message contains a substring of actual xcodebuild stderr output (proving the stderr ring-buffer is populated)

4. **Companion acceptance test (optional but valuable).** Add a UI-style test in `RemoteDeployCompanionUITests` that exercises the stuck-preflight scenario end-to-end. Requires the `--auto-pair` launch hook TKT-044 introduced. Worth considering but not strictly necessary for this ticket — the host-side test coverage plus TKT-043's wiring fix is enough to guarantee the behavior in principle.

**Why this order:** the biggest risk here is spending implementation effort on a bug that doesn't exist. Phase A is a 10-minute device check that either closes the ticket or converts it into a much more specific bug report. Phase B is worth doing either way because it improves observability and fills a genuine test gap — but it's cheap, scoped, and uncontroversial.

**Alternatives considered:**

- **Rewrite the engine to expose stderr separately from the log stream.** Could make stderr handling more explicit and testable. Rejected as overkill — the existing single-stream + "[stderr] " prefix approach is good enough, and changing it would touch every existing test.
- **Have `XcodeBuildEngine` emit `.failure` proactively on throw (vs. letting BuildManager do it).** Rejected — the engine's `setStatus(.failure(...))` is internal; only BuildManager's call to `buildEventBroadcaster.broadcastBuildStatus` fans out to subscribers. Keeping a single broadcast site (BuildManager) is the right single responsibility boundary.


## Implementation Plan

**Phase A — verify first (human task, not code):**

- [ ] **A1.** Run the companion app on a real paired iPhone with the shipped TKT-043 + TKT-044 fixes (i.e. latest `main`).
- [ ] **A2.** In RemoteDeploy host Settings → Projects, create a deliberately misconfigured project entry: `scheme=RemoteDeploy`, `platform=iOS`, pointing at this repo's `.xcodeproj`.
- [ ] **A3.** From the companion's **Build** tab, select this project and tap **Build & Deploy**. Time the response: xcodebuild should exit within ~2s.
- [ ] **A4.** Verify the Build tab transitions out of the "Building…" state to a failure state within ~3s of tapping. Button should re-enable, elapsed timer stops.
- [ ] **A5.** Verify the Prowl / push notification arrives saying the build failed.
- [ ] **A6.** Repeat A3–A5 from the **Projects → project detail** view (TKT-044's fix).
- [ ] **A7.** If both A4 and A6 pass → mark ticket closed as already-fixed, proceed to Phase B as follow-up improvements. If either fails → update the Investigation section with the exact symptom observed and convert this ticket into a targeted bug fix.

**Phase B — code changes (independent of A):**

- [ ] **B1. Add stderr ring-buffer to `XcodeBuildEngine`.**
  - File: [RemoteDeploy/Services/XcodeBuildEngine.swift](RemoteDeploy/Services/XcodeBuildEngine.swift)
  - Add a private `OSAllocatedUnfairLock<[String]>` at the top of the class for `recentStderrLines`, alongside the existing locked state.
  - In `runXcodebuild`, the stderr `readabilityHandler` at [line 295-301](RemoteDeploy/Services/XcodeBuildEngine.swift#L295-L301): for each non-empty line, append to the ring buffer under lock, trimming to last 8 entries.
  - Clear the ring buffer at the start of each `runXcodebuild` call.

- [ ] **B2. Include stderr tail in thrown error.**
  - File: [RemoteDeploy/Services/XcodeBuildEngine.swift](RemoteDeploy/Services/XcodeBuildEngine.swift)
  - In `runXcodebuild`'s termination handler at [line 323-326](RemoteDeploy/Services/XcodeBuildEngine.swift#L323-L326): replace the generic `msg` with a string that includes the last non-empty stderr line (or the last 3 lines if short). Format: `"xcodebuild failed (exit N): <last stderr line>"` falling back to `"xcodebuild failed (exit N)"` if the ring buffer is empty.
  - `BuildError.xcodebuildFailed` already takes an `Int32` and a `String`; keep the signature unchanged — just improve the message.

- [ ] **B3. Deterministic log-stream drain on error.**
  - File: [RemoteDeploy/Services/XcodeBuildEngine.swift](RemoteDeploy/Services/XcodeBuildEngine.swift)
  - In the termination handler, after calling `setStatus(.failure(...))` and BEFORE resuming the continuation with the throw, call `lockedLogContinuation.withLock { $0?.finish(); $0 = nil }` so the `for await` loop in `BuildManager.triggerBuild` exits cleanly on the next yield rather than racing the `logTask.cancel()`.
  - Do the same in the success branch for symmetry (no current bug, but keeps the lifecycle consistent).

- [ ] **B4. Unit test: ring-buffer populated on stderr lines.**
  - File: new — `RemoteDeployTests/XcodeBuildEngineFailureTests.swift`
  - Test 1: `test_runXcodebuild_withInvalidScheme_throwsWithStderrInMessage`. Construct an `XcodeBuildEngine`, call `build(project:)` with a `ProjectConfig` whose `projectPath` points at this repo's `.xcodeproj` and `scheme` is a deliberately bogus name like `"DoesNotExist"`. Assert:
    - The call throws `BuildError.xcodebuildFailed`
    - The thrown error's `errorDescription` contains a substring like `"scheme"` or `"not found"` or the literal scheme name — evidence the stderr tail made it into the message.
  - Test 2: `test_runXcodebuild_withInvalidDestination_throwsWithStderrInMessage`. Same shape, but with `scheme=RemoteDeploy` + `platform=iOS` (the real repro scenario). Assert the thrown message contains `"destination"` or `"Unable to find"`.
  - Both tests are real-xcodebuild tests — slow (1-3s each) but scoped. Mark them in a separate test class if that matters for CI perf.

- [ ] **B5. Integration test: BuildManager + real engine + WebSocket for preflight failure.**
  - File: add to [RemoteDeployIntegrationTests/BuildManagerWebSocketIntegrationTests.swift](RemoteDeployIntegrationTests/BuildManagerWebSocketIntegrationTests.swift)
  - Test: `test_triggerBuild_withRealXcodebuildPreflightFailure_broadcastsTerminalFailureFrame`. Wire up a real `XcodeBuildEngine`, a real `NIODeployServer`, a paired WebSocket client subscribed to `buildstatus`. Trigger a build with a bogus-scheme project config. Assert:
    - The WebSocket client receives a `buildstatus` frame with `state == "failure"`
    - The frame's `message` contains a substring of the real xcodebuild error (not just "exit code N")
    - The frame arrives within 5s of triggering (real xcodebuild preflight takes ~1-2s)

- [ ] **B6. Run full test suite and companion build.**
  - `xcodebuild test -project RemoteDeploy.xcodeproj -scheme RemoteDeploy -destination 'platform=macOS'` — 311 current tests + new additions must all pass.
  - `xcodebuild build -project RemoteDeploy.xcodeproj -scheme RemoteDeployCompanion -destination 'generic/platform=iOS Simulator'` — must build clean.

- [ ] **B7. Update the ticket's Files Changed + Test Report sections; set status to review.**


## Files Changed
<!-- Agent fills this during implementation -->


## Test Report
<!-- Agent fills this after implementation -->


## Verification Checklist (for human)
<!-- Agent fills this during /ticket-review -->
- [ ] 


## Regression Report
<!-- Agent fills this before merge -->
