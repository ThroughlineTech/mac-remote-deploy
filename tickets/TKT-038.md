---
id: TKT-038
title: "Remove redundant project dropdown — use project list selection instead"
type: enhancement
status: proposed
priority: medium
app: macos
branch: ""
created: 2026-04-09
updated: 2026-04-09
---

## Description

The menu bar popover has two ways to select a project:

1. **The project list** at the top — tappable rows with a small blue checkmark on the selected one.
2. **A "Project:" dropdown picker** below the project list — a standard macOS Picker control duplicating the same selection.

This is redundant and sloppy. The project list IS the selection mechanism — tapping a row selects the project. The dropdown below it does the same thing. Having both confuses the user about which is canonical and wastes vertical space in the popover.

### What to do

**Remove the "Project:" dropdown picker** from `BuildControlsSection`. The project list in `ProjectsListSection` (with its tap-to-select rows) is the primary and more natural selection mechanism.

**Make the checkmark more prominent** so it's obvious which project is selected:
- The current checkmark is a small, light-blue `Image(systemName: "checkmark")` with `.font(.caption)` and `.foregroundColor(.accentColor)` — it's subtle to the point of being invisible, especially on non-Retina displays or with reduced transparency enabled.
- Options to beef it up:
  - Use `checkmark.circle.fill` instead of plain `checkmark` — filled circle is more visible
  - Increase font size from `.caption` to `.subheadline` or `.body`
  - Use a high-contrast color (solid blue or green filled circle, not just accentColor)
  - Add a subtle background highlight (light blue tint) on the selected row, not just a checkmark — so the selection is conveyed by the row itself, not just a tiny icon
  - Bold the selected project name (already done via `.fontWeight(isSelected ? .semibold : .regular)` at [ProjectRowView.swift](RemoteDeploy/Views/MenuBar/ProjectRowView.swift)) — good, but the weight difference between `.regular` and `.semibold` is subtle at `.subheadline` size

### Where the dropdown lives

[RemoteDeploy/Views/MenuBar/BuildControlsSection.swift:15-23](RemoteDeploy/Views/MenuBar/BuildControlsSection.swift#L15-L23):

```swift
if appState.projects.count > 1 {
    Picker("Project:", selection: $appState.selectedProjectID) {
        ForEach(appState.projects) { project in
            Text(project.name).tag(project.id as UUID?)
        }
    }
    .pickerStyle(.menu)
    .font(.subheadline)
}
```

This entire `if` block can be deleted. The `.onChange(of: appState.projects)` normalization added in TKT-025 should stay (it's on the VStack, not the Picker).

### Where the checkmark lives

[RemoteDeploy/Views/MenuBar/ProjectRowView.swift](RemoteDeploy/Views/MenuBar/ProjectRowView.swift):

```swift
if isSelected {
    Image(systemName: "checkmark")
        .font(.caption)
        .foregroundColor(.accentColor)
}
```

## Acceptance Criteria
- [ ] The "Project:" dropdown picker is removed from `BuildControlsSection`
- [ ] The selected-project checkmark in `ProjectRowView` is visually prominent — easily identifiable at a glance without squinting
- [ ] Selected row has additional visual treatment beyond the checkmark (background tint, bolder text, or both)
- [ ] Tapping a project row in the list still switches the active project (existing behavior preserved)
- [ ] `.onChange(of: appState.projects)` normalization from TKT-025 stays in place
- [ ] Configuration picker (Debug/Release) still works and stays in `BuildControlsSection`
- [ ] `xcodebuild test` passes


---
<!-- Everything below is filled by the agent. Do not edit manually. -->

## Investigation

**Redundant dropdown picker** — `BuildControlsSection.swift` lines 15-23 contain a `Picker("Project:", ...)` wrapped in `if appState.projects.count > 1`. This renders a macOS `.menu`-style dropdown that duplicates the exact same selection afforded by the tap-to-select rows in `ProjectsListSection.swift` lines 17-29. Both write to `appState.selectedProjectID`. The popover layout in `MenuBarView.swift` (line 25-27) shows `ProjectsListSection` immediately above `BuildControlsSection`, separated only by a divider — so the user sees two selection mechanisms stacked vertically.

**Weak checkmark styling** — `ProjectRowView.swift` lines 25-29: the selected-project indicator is `Image(systemName: "checkmark")` with `.font(.caption)` and `.foregroundColor(.accentColor)`. At `.caption` size (~10pt), this is easy to miss. The row also uses `.fontWeight(isSelected ? .semibold : .regular)` (line 17), but the weight difference at `.subheadline` size is subtle.

**TKT-025 onChange handler** — `BuildControlsSection.swift` lines 75-82: the `.onChange(of: appState.projects)` modifier is attached to the outer `VStack`, not the `Picker`. It normalizes `selectedProjectID` when projects change. This must be preserved after the Picker is deleted — no code changes needed since it's already on the VStack.

**Regression risk: Low.** The Picker is a pure read/write binding to `appState.selectedProjectID` — removing it eliminates a code path but doesn't affect the remaining one in `ProjectsListSection`. The checkmark changes are cosmetic only. The onChange handler is structurally independent of the Picker.

## Proposed Solution

1. Delete the entire `if appState.projects.count > 1 { Picker(...) }` block (lines 15-23) from `BuildControlsSection.swift`.
2. Upgrade the checkmark in `ProjectRowView.swift` from a plain `checkmark` to `checkmark.circle.fill` at `.body` size with a solid color.
3. Add a subtle background tint to the selected row so the selection is conveyed by the row itself, not just a tiny icon.

## Implementation Plan
- [ ] 1. **BuildControlsSection.swift** — Delete lines 15-23 (the `if appState.projects.count > 1 { Picker("Project:", ...) }` block). Leave the `Picker("Configuration:", ...)` and the `.onChange(of: appState.projects)` handler untouched.
- [ ] 2. **ProjectRowView.swift** — Replace the checkmark icon: change `Image(systemName: "checkmark")` to `Image(systemName: "checkmark.circle.fill")`, change `.font(.caption)` to `.font(.body)`, change `.foregroundColor(.accentColor)` to `.foregroundColor(.blue)`.
- [ ] 3. **ProjectRowView.swift** — Add a background tint to the selected row: add `.background(isSelected ? Color.accentColor.opacity(0.10) : Color.clear)` and `.cornerRadius(4)` to the outer `HStack`. This gives the entire row a light blue wash when selected.
- [ ] 4. **ProjectRowView.swift** — Increase selected-row text weight from `.semibold` to `.bold` for stronger visual differentiation at `.subheadline` size.
- [ ] 5. Run `xcodebuild test -project RemoteDeploy.xcodeproj -scheme RemoteDeploy -destination 'platform=macOS'` and verify all tests pass.
- [ ] 6. Visually verify in the popover: only the project list rows select a project; the Configuration picker still works; selected row is clearly identifiable at a glance.

## Dependencies

None. No ticket in TKT-030 through TKT-039 modifies `ProjectRowView.swift` or the Picker block in `BuildControlsSection.swift`. TKT-039 touches the "Add Project..." SettingsLink but that is in `ProjectsListSection`, not the files changed here.

## Regression Risk

Low.

## Files Changed

- `RemoteDeploy/Views/MenuBar/BuildControlsSection.swift` — remove redundant project Picker
- `RemoteDeploy/Views/MenuBar/ProjectRowView.swift` — upgrade checkmark icon, add selected-row background tint, increase font weight

## Test Report


## Verification Checklist (for human)
- [ ] Open the menu bar popover — no "Project:" dropdown visible
- [ ] Selected project row shows a filled blue checkmark circle and light blue background
- [ ] Tapping a different project row switches the selection (checkmark + tint move)
- [ ] Configuration picker (Debug/Release segmented control) still works
- [ ] With only one project configured, the row still shows selected styling
- [ ] Delete a project — selection falls back to the first remaining project (TKT-025 onChange)


## Regression Report
